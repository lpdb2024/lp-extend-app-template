export enum SVGShapes {
  RECT = 'rect',
  CIRCLE = 'circle',
  ELLIPSE = 'ellipse',
  LINE = 'line',
  POLYLINE = 'polyline',
  POLYGON = 'polygon',
  PATH = 'path',
  TEXT = 'text',
  IMAGE = 'image',
  GROUP = 'g'
}

export enum EQUATIONS {
  ADD = 'add',
  SUBTRACT = 'subtract',
  MULTIPLY = 'multiply',
  DIVIDE = 'divide',
}

export enum ColorType {
  solid = 'solid',
  linear = 'linear',
  radial = 'radial'
}
